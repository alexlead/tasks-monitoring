export type TTaskEditModel = {
    taskId: number;
    showModal: boolean;
}

export type TTaskItem = {
    
}

export type TNewTaskSave = {
    id?: number;
    title: string;
    description: string;
}